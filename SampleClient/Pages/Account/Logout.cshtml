@page
@model LogoutModel

@{
    ViewData["Title"] = "Выход";
}

<div class="text-center py-5">
    <h1 class="h3 mb-3">Выход</h1>
    <p class="text-muted">Вы вышли из SampleClient. SSO-сессия SiMed SSO остается активной.</p>
    <a class="btn btn-primary" asp-page="/Index">На главную</a>
</div>
