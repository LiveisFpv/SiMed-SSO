@model AdminUserDetailsViewModel

@{
    ViewData["Title"] = "User details";
}

<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <h1 class="h3 mb-1">User details</h1>
        <p class="text-muted mb-0">@Model.Email</p>
    </div>
    <div class="d-flex gap-2">
        <a class="btn btn-outline-secondary" asp-action="Users">Back</a>
        <a class="btn btn-primary" asp-action="EditRoles" asp-route-id="@Model.Id">Edit roles</a>
    </div>
</div>

@if (TempData["StatusMessage"] is string statusMessage)
{
    <div class="alert alert-success" role="alert">@statusMessage</div>
}

@if (TempData["ErrorMessage"] is string errorMessage)
{
    <div class="alert alert-danger" role="alert">@errorMessage</div>
}

<dl class="row">
    <dt class="col-sm-3">User name</dt>
    <dd class="col-sm-9">@Model.UserName</dd>

    <dt class="col-sm-3">Email</dt>
    <dd class="col-sm-9">@Model.Email</dd>

    <dt class="col-sm-3">Roles</dt>
    <dd class="col-sm-9">
        @if (Model.Roles.Count > 0)
        {
            foreach (var role in Model.Roles)
            {
                <span class="badge text-bg-secondary me-1">@role</span>
            }
        }
        else
        {
            <span class="text-muted">No roles</span>
        }
    </dd>

    <dt class="col-sm-3">Active</dt>
    <dd class="col-sm-9">@(Model.IsActive ? "Yes" : "No")</dd>

    <dt class="col-sm-3">Locked out</dt>
    <dd class="col-sm-9">@(Model.IsLockedOut ? $"Yes, until {Model.LockoutEnd:u}" : "No")</dd>

    <dt class="col-sm-3">Email confirmed</dt>
    <dd class="col-sm-9">@(Model.EmailConfirmed ? "Yes" : "No")</dd>

    <dt class="col-sm-3">Phone</dt>
    <dd class="col-sm-9">@(Model.PhoneNumber ?? "Not set")</dd>

    <dt class="col-sm-3">Phone confirmed</dt>
    <dd class="col-sm-9">@(Model.PhoneNumberConfirmed ? "Yes" : "No")</dd>

    <dt class="col-sm-3">Two-factor enabled</dt>
    <dd class="col-sm-9">@(Model.TwoFactorEnabled ? "Yes" : "No")</dd>

    <dt class="col-sm-3">Failed access count</dt>
    <dd class="col-sm-9">@Model.AccessFailedCount</dd>

    <dt class="col-sm-3">Created</dt>
    <dd class="col-sm-9">@Model.CreatedAtUtc.ToString("u")</dd>
</dl>

<div class="border-top pt-3">
    @if (Model.IsActive)
    {
        <form asp-action="Deactivate" asp-route-id="@Model.Id" method="post">
            <button type="submit" class="btn btn-danger" disabled="@Model.IsCurrentUser">Deactivate user</button>
        </form>
    }
    else
    {
        <form asp-action="Reactivate" asp-route-id="@Model.Id" method="post">
            <button type="submit" class="btn btn-success">Reactivate user</button>
        </form>
    }
</div>
